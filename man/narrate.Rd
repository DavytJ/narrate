% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/narrate.R
\name{narrate}
\alias{narrate}
\title{Narrate a table: the paragraph that accompanies it in a report}
\usage{
narrate(
  table,
  example = NULL,
  instructions = NULL,
  title = NULL,
  exclude = NULL,
  generate = getOption("narrate.generate"),
  cache = TRUE,
  chat = NULL
)
}
\arguments{
\item{table}{A data frame, or a named list of data frames.}

\item{example}{A reference paragraph (e.g. last year's) that fixes style,
length and structure. Optional.}

\item{instructions}{Free-text additional instructions. Optional.}

\item{title}{Table title, included in the prompt. Optional.}

\item{exclude}{Values that are never flagged even if absent from the table.}

\item{generate}{If \code{FALSE}, never call the API (cache only).}

\item{cache}{If \code{FALSE}, neither read nor write the cache.}

\item{chat}{An \link[ellmer:Chat]{ellmer::Chat} object, a function that creates one, or an
ellmer model string such as \code{"google_gemini/gemini-3.6-flash"} (see
\code{\link[ellmer:chat]{ellmer::chat()}}). Defaults to the \code{narrate.chat} option.}
}
\value{
A \code{narrate_text} object: the text (character) with attributes
\code{verification} (see \code{\link[=verify_figures]{verify_figures()}}), \code{tokens}, \code{model} and \code{seconds}.
}
\description{
Uses a language model to write the interpretive paragraph for a table, in
the style of an \code{example} paragraph, and verifies that every figure in the
text exists in the table. Designed for inline use in R Markdown or Quarto,
right below the chunk that produces the table: \code{`r narrate(tab1)`}.
}
\details{
Results are cached in \code{narrate.cache_dir} under a key derived from the
table, the example, the instructions and the model, so re-rendering a
document does not call the API. With \code{options(narrate.generate = FALSE)}
the API is never called: if there is no cached text, an error is raised.
}
\examples{
\dontrun{
options(
  narrate.chat = "google_gemini/gemini-3.6-flash",
  narrate.language = "Spanish",
  narrate.decimals = 1,
  narrate.decimal_mark = ",",
  narrate.big_mark = "."
)
tab <- data.frame(site = c("A", "B"), n = c(1512, 1898), pct = c(22.46, 28.19))
narrate(tab, example = "En el periodo anterior se registraron 3.100 procedimientos...")
}
}
