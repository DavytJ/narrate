% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{verify_figures}
\alias{verify_figures}
\title{Verify the figures in a text against one or more tables}
\usage{
verify_figures(
  text,
  tables,
  exclude = NULL,
  decimals = getOption("narrate.decimals"),
  signif = getOption("narrate.signif"),
  decimal_mark = getOption("narrate.decimal_mark"),
  big_mark = getOption("narrate.big_mark")
)
}
\arguments{
\item{text}{A character vector (lines) or a single string.}

\item{tables}{A data frame, or a named list of data frames.}

\item{exclude}{Numeric values that are never flagged even if absent from
the tables (years, table numbers, the 95 in "95\% CI").}

\item{decimals}{Number of decimals for non-integers, or \code{NULL} to leave as is.}

\item{signif}{Significant digits for non-integers, or \code{NULL} to leave as is.}

\item{decimal_mark}{Decimal mark, e.g. \code{"."} or \code{","}.}

\item{big_mark}{Thousands separator, e.g. \code{","}, \code{"."} or \code{""}.}
}
\value{
A data frame of class \code{narrate_verification} with one row per
figure in the text: \code{figure}, \code{value}, \code{found}, \code{table}, \code{row}, \code{column},
\code{status} (\code{"ok"}, \code{"rounded"}, \code{"proportion"}, \code{"unsourced"},
\code{"excluded"}), \code{table_figure} and \code{context}. The \code{summary} attribute holds
the counts, including the number of \verb{[MISSING DATA]} markers.
}
\description{
Extracts every number from \code{text} and checks that it exists in \code{tables},
formatted exactly as the model saw them (see \code{\link[=format_table]{format_table()}}). Figures
that are not found are diagnosed as:
}
\details{
\itemize{
\item \code{"rounded"}: a table figure rounded to the decimals used in the text;
\item \code{"proportion"}: a table proportion expressed as a percentage;
\item \code{"unsourced"}: no relationship with any table figure was found.
}
}
\examples{
tab <- data.frame(site = c("A", "B"), n = c(1512, 1898), pct = c(22.46, 28.19))
verify_figures("A had 1,512 procedures (22.46\%) and B 1898 (28.2\%). Total: 3410.", tab)
}
