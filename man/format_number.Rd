% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format_number}
\alias{format_number}
\title{Format numbers the way the report prints them}
\usage{
format_number(
  x,
  decimals = getOption("narrate.decimals"),
  signif = getOption("narrate.signif"),
  decimal_mark = getOption("narrate.decimal_mark"),
  big_mark = getOption("narrate.big_mark")
)
}
\arguments{
\item{x}{A numeric vector.}

\item{decimals}{Number of decimals for non-integers, or \code{NULL} to leave as is.}

\item{signif}{Significant digits for non-integers, or \code{NULL} to leave as is.}

\item{decimal_mark}{Decimal mark, e.g. \code{"."} or \code{","}.}

\item{big_mark}{Thousands separator, e.g. \code{","}, \code{"."} or \code{""}.}
}
\value{
A character vector the same length as \code{x}.
}
\description{
Converts a numeric vector to character using the report's decimal mark,
big mark and precision. Whole numbers are never rounded. Precision is set
either as a fixed number of \code{decimals} or as \code{signif} significant digits;
when both are given, significant digits are applied first.
}
\details{
Defaults come from the options \code{narrate.decimals}, \code{narrate.signif},
\code{narrate.decimal_mark} and \code{narrate.big_mark}.
}
\examples{
format_number(c(1512, 22.46, 0.0019841), signif = 2)
format_number(c(6732, 22.46), decimals = 1, decimal_mark = ",", big_mark = ".")
}
