% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{narrate_report}
\alias{narrate_report}
\title{Reproduce a periodic report from the previous edition and new tables}
\usage{
narrate_report(
  previous,
  tables,
  period = NULL,
  instructions = NULL,
  exclude = NULL,
  generate = getOption("narrate.generate"),
  cache = TRUE,
  chat = NULL
)
}
\arguments{
\item{previous}{The previous report as a character vector (lines) or a path
to a Markdown or text file.}

\item{tables}{A named list of data frames; the names are used as table
titles in the prompt.}

\item{period}{Label for the current period, e.g. \code{"January to June 2026"}.}

\item{instructions}{Free-text additional instructions. Optional.}

\item{exclude}{Values that are never flagged even if absent from the table.}

\item{generate}{If \code{FALSE}, never call the API (cache only).}

\item{cache}{If \code{FALSE}, neither read nor write the cache.}

\item{chat}{An \link[ellmer:Chat]{ellmer::Chat} object, a function that creates one, or an
ellmer model string such as \code{"google_gemini/gemini-3.6-flash"} (see
\code{\link[ellmer:chat]{ellmer::chat()}}). Defaults to the \code{narrate.chat} option.}
}
\value{
A \code{narrate_text} object (see \code{\link[=narrate]{narrate()}}).
}
\description{
Generates the full text of a report using the previous edition as the
reference for structure and style, and the new tables as the only source
of figures. The result is verified with \code{\link[=verify_figures]{verify_figures()}}.
}
